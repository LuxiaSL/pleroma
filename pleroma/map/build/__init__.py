"""Map building: the v1a join, fit (registered CV), export, wide map, levers;
and the corpus stages under them: ``forks`` (fork positions and their
entropy), ``basin`` + ``basin_fast`` (do one prompt's seeds land in
distinguishable basins), ``hiddens`` (per-generation mean hidden states at the
lever sites), ``regress`` (can the signature predict the lever)."""
