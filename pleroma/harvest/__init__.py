"""Harvest: the anamnesis seam, the persistent harvest worker (``worker``),
its pool (``pool``), the v3 replay (``replay``), bins-B extraction (``bins``),
the GPU-lane manifests (``gpu_manifests``), and the lane-parity gate and
equivalence registry (``lane_parity``, ``lane_equivalence``)."""
