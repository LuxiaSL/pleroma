"""pleroma: convert sampled future branches of an LLM into steering levers.

The entry point is `python -m pleroma` (`pleroma.cli`). `QUICKSTART.md` walks
one model from profile to served map; `docs/API.md` specifies the loom
server's HTTP API.
"""
